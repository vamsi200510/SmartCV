'use client';

/**
 * ColorMeshBackdrop
 * Replaced by internal multi-layer gradients within the glass surfaces
 * to ensure perfect border-radius clipping with zero external rectangular seams.
 */
export function ColorMeshBackdrop() {
  return null;
}

export default ColorMeshBackdrop;
