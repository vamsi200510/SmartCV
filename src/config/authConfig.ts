/**
 * Authentication Configuration & Feature Flags
 * 
 * Centralized feature toggles for authentication flows.
 * Toggle `OTP_LOGIN_ENABLED = true` once email OTP delivery & verification is resolved.
 */
export const OTP_LOGIN_ENABLED = false;
